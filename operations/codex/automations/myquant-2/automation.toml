version = 1
id = "myquant-2"
kind = "cron"
name = "myQuant 统一因子周度健康复核"
prompt = "使用 [$myquant](/Users/maxwell/.codex/skills/myquant/SKILL.md) 在 /Users/maxwell/mySpace/myQuant 执行只读 Factor production 周度健康复核。开始前读取 AGENTS.md、.agent/CONTINUITY.md、已安装 myquant skill、operations reference、docs/factor_governance.md 和 docs/runbooks/factor_production.md；检查 branch、HEAD、dirty 状态与 import origin，保护无关改动，不做 Git 发布。\n\n先运行当前 help，再读取 `factor production-status`、`factor production-verify` 以及 LOW/W80 的 `factor production-signal`。直接读取独立 `results/factors/_active.json`、immutable genesis marker、pointer history、rollover commits 和本周 execute-mode daily-maintenance receipts。Factor ACTIVE 与 System/Mainline 未初始化可以同时成立；不得以 System pointer 缺失否认 Factor authority。W75 必须保持 CONTROL_ONLY、零权重、不可选择。\n\n只评估 sealed daily generations 的覆盖、有限性、分布、相关性、lag、eligibility、换手/成本/容量提示，以及真正存在的 registered prospective evidence。每日 generation 是 production signal history，不是 prospective admission observation；若无 exact preregistration cycle，报告 `prospective_evidence_status=NOT_CONFIGURED`，不得编造 IC、RankIC、1D/5D/20D/60D outcome、maturity 或 graduation。Macro/Fundamental blocker保持披露，但当 active LOW/W80 不消费它们时不得反向阻断 Factor production health。\n\n保持完全只读：不得调用 production-rollover、production-activate、system activate、provider、registry apply、mainline publish、portfolio/Paper/Strategy Record writer、broker、订单或交易。最终报告当前 generation/as-of、pointer/marker/chain SHA、LOW/W80 signal 与 symbol-set SHA/count、本周 rollover/no-action/blocked receipts、Factor lag、pending frozen-scope expansion、Macro/Fundamental limitations、protected bytes 与实际 tests。明确 active_generation_write=false、system_authority=false、mainline_authority=false、trading_authority=false。"
status = "PAUSED"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=SA;BYHOUR=6;BYMINUTE=30;BYSECOND=0"
model = "gpt-5.6-sol"
reasoning_effort = "high"
execution_environment = "local"
target = { type = "project", project_id = "local-f9bd22cbbeff01393d5315615e57cb9a" }
cwds = ["/Users/maxwell/mySpace/myQuant"]
created_at = 1780035656867
updated_at = 1787261400000
